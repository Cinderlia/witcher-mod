请参照项目中，对sql注入进行符号执行的一般流程，追加对反射型xss注入的符号执行流程

第一步：识别潜在的注入点

echo ...
print ...
printf ...
<?= ... ?>

请识别以上四种模式，需要结合AST树进行识别，不要只进行模式匹配

识别的时候请保证if（else if）识别和switch识别和sql识别和xss识别并行执行
对同一句依次进行上述识别，命中之后就执行对应的操作，全部miss就直接下一句，避免扫描两次

AST的节点在input\nodes.csv，边在input\rels.csv文件中

识别到注入点之后，就加入缓冲区。和sql注入一样，xss注入也需要一个独立的缓冲区。缓冲区满或者所有seq扫描完成后，组成prompt，进行LLM调用

相关实现参考对sql注入点的处理，区别仅在缓冲区不共享、prompt不同。

之后在调用analyze_if_line.py的时候，也需要传入-xss以表示这是xss注入点，初始污点的提取采用提取上述四种结构里面省略号部分里面的所有，我们接受的变量（AST_VAR、AST_PROP等，提取规则和sql注入点、if语句一样，可以直接复用那边的代码）

后续污点扩散和if语句，sql注入点一模一样

前面的筛选prompt和最后的symbolic_prompt都采用特殊prompt，你先复制sql注入的过去，我等下会进行微调

适当输出些日志方便调试

代码高内聚低耦合，多封装函数，尽量新建python文件，不要什么代码都往一个大文件里面写。有什么疑惑的，就参考sql注入点的处理方案。

最后运行一次python main.py，预期结果是正确筛选出xss注入点，并形成prompt（因为是test模式，所以不会调用LLM，但是有模拟返回），然后调用analyze_if_line.py